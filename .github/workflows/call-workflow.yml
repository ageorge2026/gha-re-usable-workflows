name : Call Reusable Workflow
on:
  push:
jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/reusable.yml
    with:
      name: 'Alice'  # Pass the name input to the reusable workflow
